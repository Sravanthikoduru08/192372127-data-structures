#include <stdio.h>

#define ROW 3
#define COL 3

int main() {
    int matrix[ROW][COL] = {{1, 2, 3}, {4, 15, 6}, {7, 8, 12}};
    int max = matrix[0][0];

    for (int i = 0; i < ROW; i++) {
        for (int j = 0; j < COL; j++) {
            if (matrix[i][j] > max) {
                max = matrix[i][j];
            }
        }
    }
    printf("192372127_ sravanthi\n");
    printf("The largest element in the matrix is: %d", max);
return 0;
}
